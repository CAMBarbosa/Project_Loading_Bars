import React from "react";

const ComplexProgressBarPage = () => {
  return (
    <div>
      <h1>
        Here are more comples examples of a progress bars. You can choose the
        image that builds up the progress bar in a menu. You also can increment
        the progress bar manually using the "Speed Up" button, or to see a time
        dependant progress bar, using the "Let Time Fly" button.
      </h1>
    </div>
  );
};

export default ComplexProgressBarPage;
